services:
  - type: web
    name: voting-backend
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/voting-system-1.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
    plan: free
